# Planning rule

Before implementing any non-trivial feature, refactor, or design change, invoke `/grill-me` to stress-test the plan. Skip only for obvious one-liners or bug fixes with a clear root cause.

This ensures AI and human reach shared understanding on scope, tradeoffs, and approach before any code is written.
