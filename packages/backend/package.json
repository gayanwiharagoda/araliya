{
  "name": "@domus/backend",
  "private": true,
  "scripts": {
    "dev": "npx convex dev",
    "lint": "eslint --ignore-pattern convex/_generated .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "convex": "^1.42.0"
  },
  "devDependencies": {
    "convex-test": "^0.0.53",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
